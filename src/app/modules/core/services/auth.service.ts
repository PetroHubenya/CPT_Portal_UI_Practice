import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {

  constructor() { }

  // Initiate a login (user authentication) process by integrating with Microsoft's Azure AD through the MSAL (Microsoft Authentication Library).
  login(){}
}
